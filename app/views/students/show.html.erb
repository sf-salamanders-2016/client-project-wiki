<h1>Student Appointments</h1>

<div class="open-appointments">
  <h2>Open Slots</h2>
  <% @appointments.each do |appointment| %>
    <ul>
      <a href="/appointments/<%= appointment.id %>"><li>Start Time: <%= appointment.start_time  %><br>Duration: <%= appointment.duration %></li></a>
    </ul>
  <% end %>
</div>

<div class="set-appointemts">
  <h2>My Appointments</h2>
  <% @student.appointments.each do |appointment| %>
    <ul>
      <a href="/appointments/<%= appointment.id %>"><li>Start Time: <%= appointment.start_time  %><br>Duration: <%= appointment.duration %></li></a>
    </ul>
  <% end %>
</div>

<div class="complete-appointemts">
  <h2>Completed Appointments</h2>
  <% @complete.each do |appointment| %>
    <ul>
      <a href="/appointments/<%= appointment.id %>"><li>Start Time: <%= appointment.start_time  %><br>Duration: <%= appointment.duration %></li></a>
    </ul>
  <% end %>
</div>
